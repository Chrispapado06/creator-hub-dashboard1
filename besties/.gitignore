# Runtime-uploaded images live on the server/volume, not in git
images/*
!images/.gitkeep

# runtime analytics counts live on the server/volume, not in git
analytics.json

# temp files from atomic writes
data.json.tmp
analytics.json.tmp

node_modules/
